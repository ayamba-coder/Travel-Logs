/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "parcelmonitorlogistics.com",
      "images.unsplash.com",
      "plus.unsplash.com",
      "expressdove.com",
    ],
  },
};

module.exports = nextConfig;
